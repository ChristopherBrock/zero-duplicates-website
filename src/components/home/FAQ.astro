---
import { getLocaleFromUrl } from '../../i18n/ui';
import { translations } from '../../i18n/translations';

const currentLang = getLocaleFromUrl(Astro.url);
const t = translations[currentLang];
---

<section id="faq" class="py-24 bg-background-muted">
  <div class="container-width">
    <div class="mx-auto max-w-2xl lg:text-center">
      <h2 class="text-3xl font-bold tracking-tight text-white sm:text-4xl">
        {t.faq.title}
      </h2>
      <p class="mt-6 text-lg leading-8 text-text-secondary">
        {t.faq.subtitle}
      </p>
    </div>

    <dl class="mx-auto mt-16 max-w-2xl space-y-8">
      <div class="bg-background-elevated px-6 py-8 rounded-lg border border-white/10">
        <dt class="text-lg font-semibold leading-7 text-white">
          {t.faq.questions.howItWorks.question}
        </dt>
        <dd class="mt-4 text-base leading-7 text-text-secondary">
          {t.faq.questions.howItWorks.answer}
        </dd>
      </div>

      <div class="bg-background-elevated px-6 py-8 rounded-lg border border-white/10">
        <dt class="text-lg font-semibold leading-7 text-white">
          {t.faq.questions.isSafe.question}
        </dt>
        <dd class="mt-4 text-base leading-7 text-text-secondary">
          {t.faq.questions.isSafe.answer}
        </dd>
      </div>

      <div class="bg-background-elevated px-6 py-8 rounded-lg border border-white/10">
        <dt class="text-lg font-semibold leading-7 text-white">
          {t.faq.questions.fileTypes.question}
        </dt>
        <dd class="mt-4 text-base leading-7 text-text-secondary">
          {t.faq.questions.fileTypes.answer}
        </dd>
      </div>
    </dl>
  </div>
</section>